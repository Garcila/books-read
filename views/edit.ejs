ls
<% include partials/header %>


<h1>EDIT</h1>
<div class="ui main">

  <h1>new</h1>

  <form class="" action="/books/<%= book._id %>?_method=PUT" method="POST">
    <input type="text" name="book[title]" value="<%= book.title %>">
    <input type="text" name="book[author]" value="<%= book.author %>">
    <input type="date" name="book[published]" value="<%= book.published %>">
    <input type="text" name="book[rating]" value="<%= book.rating %>">
    <textarea name="book[body]" ><%= book.body %></textarea>
    <input type="text" name="book[image]" value="<%= book.image %>">
    <input type="submit">
  </form>

  <h1>New page</h1>
</div>

<% include partials/footer %>
